import React from 'react';
import styled from 'styled-components';

const Home = () => {
    return (
        <Container>
            <h1>Home Page</h1>
        </Container>
    );
};

export default Home;

const Container = styled.section``;
